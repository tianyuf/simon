# Herbert Simon Papers - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# AI Analysis API Keys (required for paper analysis features)
# =============================================================================

# DeepSeek API key (primary analysis provider)
# Get from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Anthropic API key (fallback analysis provider)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# Cloudflare R2 Storage (optional, for PDF mirroring)
# =============================================================================

# Cloudflare R2 Account ID
# Found in R2 dashboard URL: https://dash.cloudflare.com/[ACCOUNT_ID]/r2
R2_ACCOUNT_ID=your_r2_account_id

# R2 Access Key ID (create in R2 > Manage R2 API Tokens)
R2_ACCESS_KEY_ID=your_r2_access_key

# R2 Secret Access Key
R2_SECRET_ACCESS_KEY=your_r2_secret_key

# R2 Bucket Name
R2_BUCKET_NAME=simon

# Public URL for serving R2 files (optional - can use Cloudflare's default)
# Example: https://your-bucket.your-account.r2.cloudflarestorage.com
# Or use a custom domain: https://library.simon.example.com
R2_PUBLIC_URL=https://your-r2-public-url

# =============================================================================
# Authentication (optional - leave empty for no authentication)
# =============================================================================

# Admin username and password for web interface login
# If not set, the site will be publicly accessible
# AUTH_USERNAME=admin
# AUTH_PASSWORD=your_secure_password_here

# Flask secret key for session encryption (required if auth is enabled)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=your_random_secret_key_here

# =============================================================================
# Application Configuration (VPS deployment)
# =============================================================================

# Server host binding
# - Use 127.0.0.1 for local development (default)
# - Use 0.0.0.0 for VPS deployment (binds to all interfaces)
HOST=127.0.0.1

# Server port
# - Use 5000 for local development (default)
# - Use 8001 for production (matches systemd service)
PORT=5000

# Debug mode (set to 'true' for development, leave empty for production)
# DEBUG=true

# URL prefix for subdirectory deployment (e.g., '/simon' for https://example.com/simon)
# Leave empty for root deployment
URL_PREFIX=

# Database path (optional - defaults to db/simon_papers.db)
# Use absolute path for VPS deployment
# DATABASE_PATH=/var/www/simon/db/simon_papers.db

# =============================================================================
# VPS Deployment Notes
# =============================================================================
#
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual API keys and settings
# 3. For VPS deployment, set HOST=0.0.0.0 and PORT=8001
# 4. Set DATABASE_PATH to an absolute path outside the web root
# 5. Ensure the database directory is writable by the web server user (www-data)
# 6. Never commit .env to git - it should already be in .gitignore
#
# Example VPS .env:
#   HOST=0.0.0.0
#   PORT=8001
#   DATABASE_PATH=/var/lib/simon/simon_papers.db
#   DEEPSEEK_API_KEY=sk-...
#   ANTHROPIC_API_KEY=sk-ant-...
#   R2_ACCOUNT_ID=...
#   (etc)
#
# =============================================================================
